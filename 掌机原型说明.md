# FastWrite 掌机原型 · 功能与交互说明

> 对应文件：`Handheld-prototype`（源文件）
> 本文档描述原型中**已实现**的全部元素、状态与交互行为，以及为演示所做的模拟处理。

---

## 1. 整体结构

竖向复古掌机，居中显示，不占满屏幕。机身为深灰金属渐变外壳，圆角上方 26px / 下方 44px，带高光内描边、深阴影和两颗底部十字螺丝。

机身自上而下：

| 区域 | 内容 |
|---|---|
| 卡带槽 | 插着一张 `HOTWORDS` 卡带，可点击 |
| 屏幕 | 文字显示区 + 波形条 + 状态栏 |
| 丝印行 | 左 `FW-01` / 右 `ASR ENGINE` |
| 操作区 | 左：圆盘十字录制键；右：LOCAL/WEB 引擎拨杆 + 菱形四键 |
| 历史卡槽 | `SLOT 01—03` 三条记录 + `CLEAR` |
| 底部 | 居中丝印 `Created by WeaPoN` + 两颗螺丝 |

机身整体是一个 3D 翻面容器：正面为主界面，背面为热词表。

---

## 2. 屏幕

外层为凹陷屏框（内阴影），屏幕本体为暗绿黑底 + 琥珀橙文字（`#ffb066`，带发光文字阴影），叠加两层复古效果：

- **扫描线**：全屏 1px/3px 的横向暗纹，静态。
- **扫描光带**：一条半透明暖光带，5.5s 循环自上而下滑过。

屏幕自上而下分三层：

### 2.1 文字显示区（高 300px，可滚动）
- **final 文本**：琥珀橙，已锁定的识别结果。
- **interim 文本**：暗棕色（`#7d6a4e`），实时未定稿部分，边说边追加。
- **光标**：仅录制中显示，8×15px 实心方块，1s 步进闪烁。
- 录制中自动滚动到底部，始终看得见最新的字。
- 文字支持自动换行（`word-break` + `text-wrap: pretty`）。

### 2.2 波形条（高 26px）
- 28 根竖条，底部对齐，绿色（`#3f6a48`）。
- 数据来自**真实麦克风**：`getUserMedia` → `AudioContext.createAnalyser()`（fftSize 128），逐帧取频谱，映射为 2–22px 高度。
- 停止录制后全部归位为 2px 静止状态。
- 无麦克风权限时静默保持静止，不报错。

### 2.3 状态栏（屏幕底部，点阵字体 DotGothic16）
左侧为状态圆点 + 状态名，右侧为热词标识与字数。

| 状态 | 文案 | 圆点色 | 触发时机 |
|---|---|---|---|
| idle | 待机 | 暗绿 `#5d7a63` | 初始 / 取消 / 复制提示结束后 |
| recording | 录制中 | 橙红 `#ff4d2b` | 按下十字键或空格开始 |
| asr | 识别中 | 琥珀 `#ffb066` | 结束录制后约 620ms 的收尾阶段 |
| ai | AI处理 | 琥珀 `#ffb066` | 按下 FIX / POL 后约 1100ms |
| copied | 已复制 | 绿 `#4ddb7a` | 写入剪贴板成功，2.2s 后自动回到待机 |

右侧信息：
- **热词标识**：热词表非空时显示 `▭ HW{数量}`（琥珀色），为空则隐藏。
- **字数**：当前屏幕文字总字数（final + interim）。
- **引擎标识**：显示当前档位 `LOCAL` 或 `WEB`；当拨杆在 LOCAL 而本地引擎不可用时，变红显示 `LOCAL ×`，实际走浏览器识别（自动降级）。悬停有说明文案。

### 2.4 屏幕顶部信息条（屏框内、屏幕上方）
- 左侧：`FASTWRITE` 丝印。
- 右侧：**LOCAL 引擎指示灯** + `LOCAL` 标签。绿色发光 = 本地 FunASR 引擎可用；熄灭（暗灰）= 不可用，标签同步压暗。

---

## 3. 卡带

- 位于屏幕上方，造型为「插入槽 + 露出的卡带体」：上方深色卡槽带内阴影，下方是米褐色卡带体，两侧两块深色触点，中间丝印 `HOTWORDS`。
- **hover**：卡带体略微向下伸出（高度 34→38px）并提亮，模拟"抽出/推入"的手感。
- **点击**：整机沿 Y 轴翻转 180°（0.75s 缓动），进入背面的热词表。

---

## 4. 操作区

### 4.1 左侧：圆盘十字录制键
- 造型：外层圆形凹槽（内阴影）→ 内层圆形凸台 → 上方十字键帽（横竖两条圆角矩形叠成）。
- **整块可点击**，功能为「开始 / 结束录制」。
- 中心显示当前动作标签：待机时 `REC`（灰），录制中 `STOP`（白）。
- **录制中**：十字键帽由深灰变为橙红渐变，并带外发光。
- 快捷键：`空格` 等效点击（输入框聚焦时不触发）。

### 4.2 右侧：引擎拨杆 + 菱形四键

**LOCAL / WEB 拨杆**（位于菱形四键正上方，与四键居中对齐）
- 造型：126×22px 胶囊形凹槽，内嵌一枚带 5 道滚花纹的金属拨柄，两档丝印 `LOCAL`（左）/ `WEB`（右）。
- 点击整体在两档间切换，拨柄以 0.18s 缓动滑到另一端，当前档位丝印高亮、另一档压暗。
- `LOCAL` = FunASR 本地引擎；`WEB` = 浏览器识别（WebSpeechProvider，兜底）。
- 本地引擎不可用时，`LOCAL` 丝印变红，屏幕状态栏同步显示 `LOCAL ×`，实际识别自动走 WEB。
- 该开关不影响已有文本与历史记录，仅决定下一次录制使用哪个引擎。

**菱形四键**
底衬为一块 45° 旋转的圆角方形凹板，四枚圆形按键呈菱形分布，全部为英文标签，均带 4px 立体投影 + 按下位移 3px 的物理反馈。

| 位置 | 标签 | 颜色 | 功能 |
|---|---|---|---|
| 上 | `FIX` | 深灰 | AI 纠错：修正同音错别字、按热词回正 |
| 右 | `POL` | 橙 | AI 润色：去口头禅、理顺语序 |
| 下 | `CPY` | 深灰 | 手动复制当前屏幕文字到剪贴板 |
| 左 | `RAW` | 橙 | 原文 / AI 版一键来回切换 |

**FIX / POL 行为**：以当前的原始识别文本为输入（若已处理过，则始终以原文为基准，避免叠加劣化），状态转为「AI处理」约 1.1s，结果写回屏幕、覆盖最新一条历史记录、并自动复制。空文本或录制中时不响应。

**RAW 行为**：仅在本轮已做过 AI 处理后生效，在原文与 AI 版之间切换屏幕显示，不改动历史记录、不触发复制。

**CPY 行为**：把屏幕当前文字写入剪贴板，状态转「已复制」。

---

## 5. 历史记录（SLOT 01—03）

机身下部常驻三个卡槽，模拟卡带插槽的横向长条。

- 每条显示：**时间**（点阵字体 HH:MM:SS）+ **文字**（单行截断，超出显示省略号）+ **标签**。
- 标签取值：`ASR`（原始识别）/ `FIX`（已纠错）/ `POLISH`（已润色）。
- 空槽显示 `--:--:--` / `空卡槽`，整体压暗。
- **当前选中项**：橙色描边 + 提亮底色。
- **点击一条**：把该条文字载入屏幕，并重新复制到剪贴板（状态转「已复制」）。
- **CLEAR**：清空全部三条记录（hover 变橙）。
- 容量固定为最近 3 条，新记录从顶部插入，超出自动挤掉最旧一条。
- **AI 覆盖规则**：按 FIX / POL 后，最新一条记录被处理后的文字**原地覆盖**（时间刷新、标签变更），与屏幕内容保持一致，不新增记录。

---

## 6. 背面：热词表

点击卡带后翻面进入，背板同样是深灰金属，含返回按钮、词表区与电池仓。

- **标题行**：`HOTWORD CARTRIDGE` + `返回` 按钮（点击翻回正面）。
- **计数行**：`一行一个词 · 共 N 条`（点阵字体）。
- **词条列表**（可滚动）：每行为 `序号 + 输入框 + 删除按钮`。
  - 序号为 `01`、`02`… 点阵字体。
  - 输入框为琥珀色文字暗底，聚焦时描边变橙。
  - 删除按钮 `×`，hover 变橙红实心。
- **新增一行**：底部虚线框按钮 `＋ 新增一行`，hover 变橙。
- 词表变化会**立即反映到正面状态栏**的 `HW{数量}` 标识。
- 预置词条：张伟 / 混沌工程 / FastWrite。
- **电池仓**：底部 `BATT · 4×AA` 丝印 + 两颗十字螺丝，纯造型。

---

## 7. 快捷键

| 按键 | 行为 |
|---|---|
| `空格` | 开始 / 结束录制（背面或输入框聚焦时不触发） |
| `Esc` | 取消本次录制：停止麦克风与识别，清空屏幕，回到待机，**不复制、不入历史** |

（引擎拨杆无快捷键，仅鼠标点击。）

---

## 8. 完整交互流程

1. **待机** → 点十字键 / 按空格
2. **录制中**：麦克风开启，波形跳动，屏幕边说边出字（interim 暗色 → final 亮色），光标闪烁
3. 再点一次 / 再按空格 → **识别中**（约 0.6s 收尾）
4. 文字落定 → 写入历史 SLOT 01（标签 `ASR`）→ 自动复制 → **已复制**（2.2s 后回待机）
5. （可选）按 `FIX` 或 `POL` → **AI处理**（约 1.1s）→ 屏幕与 SLOT 01 同步更新 → 自动复制
6. （可选）按 `RAW` 对照原文；按 `CPY` 再次复制；点历史卡槽回看并复制某条
7. （可选）点卡带翻面维护热词，返回后立即生效
8. （任意时刻）拨动 LOCAL / WEB 切换下一次录制使用的识别引擎

---

## 9. 原型的真实能力与模拟部分

**真实实现**
- 麦克风采集与频谱波形（`getUserMedia` + `AnalyserNode`）
- 语音识别（浏览器 Web Speech API，`zh-CN`，continuous + interimResults）
- 剪贴板写入（`navigator.clipboard.writeText`）
- 键盘快捷键、状态机、历史容量与覆盖逻辑、热词表增删改

**模拟实现（用于无环境时演示）**
- **无语音识别接口或识别报错时**：自动播放一段预置口述文本的逐字上屏动画，流程其余部分完全一致。
- **AI 纠错**：本地词映射表（如 章伟/张微 → 张伟，混沌工成/浑沌工程 → 混沌工程），非真实 LLM。
- **AI 润色**：本地正则去除口头禅（嗯、啊、那个、就是说、然后呢、这个这个）并归并多余逗号，非真实 LLM。
- **处理耗时**：AI 1.1s、识别收尾 0.6s 为固定延时，用于展示状态流转。

**未接入**：本地 FunASR 引擎实体、后端热词持久化、真实 LLM 代理、首次启动进度。

**引擎可用性开关**：本地引擎在线与否目前由一个可调参数 `localEngineAvailable`（布尔）控制，便于演示两种状态；接真后端时替换为心跳探测即可。它同时驱动屏幕顶部的 LOCAL 指示灯、拨杆丝印颜色与状态栏的 `LOCAL ×` 降级提示。

---

## 10. 设计规格摘要

| 项 | 值 |
|---|---|
| 机身宽度 | 452px |
| 屏幕文字区高 | 300px |
| 正文字体 | Noto Sans SC，15px / 1.85 |
| 点阵字体 | DotGothic16（状态栏、时间、序号、计数） |
| 丝印字体 | Barlow Condensed（全大写 + 字距 0.24–0.3em） |
| 机身色 | `#4a4f55` → `#212328` 渐变 |
| 屏幕底色 | `#0d1310` → `#111a13` |
| 屏幕文字 | `#ffb066`（final） / `#7d6a4e`（interim） |
| 主按键橙 | `#c9612f` → `#a1441c`；录制态 `#e0552b` → `#a5300f` |
| 状态绿 | `#4ddb7a`（已复制） / `#5d7a63`（待机） |
| 翻面动画 | 0.75s `cubic-bezier(.6,.05,.25,1)` |
| 引擎拨杆 | 126×22px 胶囊，拨柄 60×18px，滑动 0.18s `cubic-bezier(.5,0,.2,1)` |
| 引擎指示灯 | 可用 `#4ddb7a` + 外发光；不可用 `#3a3f45` 无光 |
