# FastWrite 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名 | FastWrite · 语音速记（掌机版） |
| 版本 | v0.3 |
| 状态 | M3/M4 已完成，待 M2/M5 |
| 定位 | 本地优先的个人写作速记工具，界面为复古掌机形态 |

---

## 1. 产品概述

### 1.1 一句话定位
**说一段，写一段**——一台放在眼前的掌机，对着它口述就会实时变字，说完自动复制，直接粘贴进文档。识别本地化、热词可定制、AI 辅助纠错/润色，双击即用、隐私可控。

### 1.2 目标用户与核心场景
- **核心用户**：需要大量文字输入的写作者（文章、报告、文案、邮件）。
- **核心场景**：**想一段说一段**——用户面向掌机口述，屏幕实时逐字显字，停下自动落定，结束即自动复制，满意按 `CPY` 或直接去文档 `Ctrl+V`。
- **界面形态**：一台复古掌机（Handheld），非传统网页卡片。当前版本**不做**会议/访谈长时段多说话人记录（说话人分离不在本期范围）。

### 1.3 核心价值（按优先级）
1. **本地识别，隐私可控**：音频默认不出本机（FunASR 本地引擎）。
2. **热词定制**：人名、术语、领域词在卡带背面维护，解决中文识别最大痛点。
3. **AI 辅助**：识别后按 `FIX`（纠错）/ `POL`（润色）手动处理，处理结果基于原始识别文本。
4. **零依赖兜底**：无 Python 环境时自动降级浏览器 Web Speech API，功能不中断。

---

## 2. 需求确认结论（已与用户确认）

| 项 | 结论 |
|---|---|
| 界面形态 | **复古掌机**（Handheld），竖向机身、屏幕 + 物理按键 + 历史卡槽 + 卡带翻面 |
| 核心场景 | 个人写作速记（想一段说一段），说完自动复制 |
| 语言 | 仅中文（本地引擎只支持中文，无语言切换） |
| 热词管理 | 掌机卡带背面表单维护（每行一词 + 增删，立即保存生效） |
| LLM 能力 | 菱形键 **FIX 纠错** / **POL 润色**，手动触发，每次以**原始 ASR 文本**为输入（避免叠加劣化） |
| LLM 提供方 | 云端 API 优先（DeepSeek/OpenAI 兼容），本地 Ollama 可选配 |
| AI 处理模型 | **按键式**：识别后用户按 FIX/POL 才处理，不设持久自动模式 |
| 屏幕文字 | **只读**：掌机只显示与复制，改字去目标文档（无屏内编辑） |
| 历史记录 | 材质化：**SLOT 01—03 三个固定卡槽**，会话内（刷新清空），点击重复制，无「复制全部」 |
| 平台 | 先 Windows |
| 界面主题 | 掌机机身自带的暗绿屏 + 琥珀字（非传统亮/暗主题切换） |
| 前端技术 | CSS + HTML + 原生 JS（不上 React/Vue），可编辑单文件交付 |
| Web Speech API | 仅作兜底（预计 ~5% 场景），无热词/AI 能力，FIX/POL/RAW 置灰并说明 |

---

## 3. 功能需求

### FR-1 语音识别（本地主引擎）
- **FR-1.1** 主引擎：FunASR / Paraformer-zh-streaming（流式），**边说边出字**：interim 暗棕实时回显 + final 琥珀锁定。
- **FR-1.2** 自动分段：VAD 检测停顿，段落后自动落定（行为对齐现有分段体验）。
- **FR-1.3** 识别文本自动带标点（FunASR 标点恢复）。
- **FR-1.4** 热词注入：识别请求携带热词表，解码层生效（FR-2）。
- **FR-1.5** 兜底引擎：无 Python 后端时自动降级 `WebSpeechProvider`，功能可用但无热词/本地能力。
- **FR-1.6** 引擎切换对用户透明：无需手动选择，后端在线即本地引擎，离线自动兜底。

### FR-2 热词管理（卡带背面）
- **FR-2.1** 点击顶部 HOTWORDS 卡带整机翻转 180°，背面维护热词表：每行一个词、新增一行、逐行删除。
- **FR-2.2** 修改立即生效：保存后对后续识别请求即生效，无需重启（后台 blur 自动保存到后端 hotwords.json）。
- **FR-2.3** 词表即时反映到正面状态栏 `▭ HW{数量}` 标识。
- **FR-2.4** 预置词条：张伟 / 混沌工程 / FastWrite。

### FR-3 LLM 代理（按键式）
- **FR-3.1** 菱形键手动触发，两个模式：
  - **FIX 纠错**：修正错别字与热词误识别（结合热词表提示）。
  - **POL 润色**：去口头禅（嗯、啊、那个、就是说）、理顺语序，保持原意。
- **FR-3.2** 每次处理**均以原始 ASR 识别文本为输入**（已处理过也以原文为基准，避免叠加劣化）；
  处理结果**写回屏幕 + 自动复制 + 原地覆盖历史最新一条卡槽**并改标签（FIX / POLISH）。
- **FR-3.3** 提供方配置：支持 OpenAI 兼容 API（DeepSeek、OpenAI、其他）或本地 Ollama；API Key 仅存后端，前端不暴露。
- **FR-3.4** 隐私提示：LLM 处理会将识别出的**文本**发送至所选服务，界面在可用时明确可感；音频始终不出本机。
- **FR-3.5** 兜底：LLM 不可用（无配置/网络失败）时静默跳过/保留原文，不影响主流程；空文本或录制中不响应。

### FR-4 会话内历史（SLOT 卡槽）
- **FR-4.1** 机身下部常驻 SLOT 01—03 三个卡槽，容量固定最近 3 条，新条目从顶部插入，超出挤掉最旧。
- **FR-4.2** 每条显示时间(HH:MM:SS) + 文本（单行截断）+ 标签（ASR / FIX / POLISH）；点击某条载入屏幕并重新复制。
- **FR-4.3** `CLEAR` 清空全部三槽；刷新页面即清空（会话内，不持久化）。
- **FR-4.4** AI 覆盖规则：按 FIX/POL 后，最新一条被处理文字**原地覆盖**（时间刷新、标签变更），不新增记录。
- **FR-4.5** UI 需让人知道「这是本次的，关掉就没了」，避免误解成永久保存。

### FR-5 掌机视觉与交互
- **FR-5.1** 机身：深灰金属渐变外壳、上方 26px / 下方 44px 圆角、高光内描边、深阴影、底部两颗十字螺丝；居中不占满屏。
- **FR-5.2** 屏幕：暗绿黑底 (`#0d1310→#111a13`) + 琥珀字 (`#ffb066`)，叠加扫描线 + 5.5s 循环光带；final 亮橙 / interim 暗棕 (`#7d6a4e`)；录制中显示 8×15px 闪烁光标。
- **FR-5.3** 真实波形：`getUserMedia` + `AudioContext.createAnalyser()` 采集麦克风真实音量，驱动 28 条绿色竖条（2–22px）；停止后归位 2px 静止；无权限时静默静止不报错。
- **FR-5.4** 状态栏（屏幕底部，点阵字体）：左侧状态圆点 + 状态名，右侧热词标识 + 字数。五态：
  **待机**(暗绿) / **录制中**(橙红) / **识别中**(琥珀) / **AI 处理**(琥珀) / **已复制**(绿)。
- **FR-5.5** 操作区：**左侧圆盘十字录制键**（整块可点，待机 REC 灰 → 录制 STOP 白，录制中十字变橙红发光）；**右侧菱形四键** `FIX`(上/纠错) `POL`(右/润色) `CPY`(下/复制) `RAW`(左/原文↔AI 切换)，各带 4px 立体投影 + 3px 按压位移。
- **FR-5.6** 卡带回翻：顶部 HOTWORDS 卡带 hover 微伸、点击整机沿 Y 轴 180° 翻转（0.75s 缓动）进入背面热词表。
- **FR-5.7** 屏幕文字只读：不提供屏内编辑，修改放目标文档。

### FR-6 快捷键与防误
- `空格`：开始 / 结束录制（卡带背面或输入框聚焦时不触发）。
- `Esc`：取消本次——停止麦克风与识别、清空屏幕、回待机，**不复制、不入历史**（toast 提示「已取消，未复制」）。
- 窗口 `blur`：录制中切走窗口自动停止（防误录一堆空白）。

---

## 4. 非功能需求

| 编号 | 类别 | 要求 |
|---|---|---|
| NFR-1 | 性能 | 本地流式识别**首字延迟 < 500ms**（CPU）；识别速度实时率 RTF < 0.5 |
| NFR-2 | 性能 | 音频端到端延迟低：采集 → WebSocket → 回显链路无感 |
| NFR-3 | 隐私 | 本地识别时音频**不出本机**；仅按 FIX/POL 时发送文本，明示 |
| NFR-4 | 兼容 | Windows 10/11 + Chrome/Edge；无 Python 环境时 Web Speech 兜底可用 |
| NFR-5 | 部署 | 「双击即用」：FastWrite-Launcher.bat 一键拉起 Python 后端（存在时） |
| NFR-6 | 合规 | 确认 FunASR 框架与 paraformer-zh-streaming 权重许可证可商用 |
| NFR-7 | 可维护 | 前端可编辑单文件（当前 Handheld.html），M2 将拆模块 + 单文件构建 |

---

## 5. 技术架构

```
┌──────────────────────────────────────────────────────┐
│ 浏览器（FastWrite 掌机前端，单 HTML = Handheld.html）   │
│  ┌────────────────────────────────────────────────┐  │
│  │ ASR Provider 抽象层                              │  │
│  │  ├─ LocalASRProvider  ← 主（WebSocket → 后端）   │  │
│  │  └─ WebSpeechProvider ← 兜底（~5% 场景）          │  │
│  ├─ 掌机机身 / 屏幕（interim+final+光标）             │  │
│  ├─ 真实波形（AnalyserNode → 28 条）/ 扫描线光带      │  │
│  ├─ 十字录制键 / 菱形 FIX·POL·CPY·RAW / 状态栏五态    │  │
│  └─ 卡带翻面 ← 背面热词表 / SLOT 01-03 历史           │  │
└───────────────┬────────────────────────────────────┘
                │ HTTP 静态        WebSocket 流式音频
┌───────────────▼────────────────────────────────────┐
│ Python 后端（FastAPI + Uvicorn，Windows）            │
│  ├─ GET /            掌机前端（Handheld.html）       │
│  ├─ WS /ws/asr       流式 PCM → FunASR（流式+VAD）   │
│  │                    ← interim/final 文本 + 标点     │
│  ├─ POST /hotwords   热词表管理（持久化到文件）       │
│  └─ POST /rewrite    LLM 代理（DeepSeek/Ollama，Key  │
│                      仅存后端；FIX/POL 基于原始文本）  │
└────────────────────────────────────────────────────┘
```

### 5.1 关键技术选型
| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | FastAPI + Uvicorn | 异步、WebSocket 一等支持；一个进程搞定静态+API+WS |
| ASR 引擎 | FunASR + paraformer-zh-streaming | 中文主场、原生热词、流式、CPU 实时、标点恢复 |
| 分段 | FunASR VAD（fsmn-vad） | 停顿自动分段，对齐现有体验 |
| 字符集 | DotGothic16（点阵）+ Noto Sans SC（正文） | 掌机像素风状态栏 + 中文正文 |
| LLM 代理 | OpenAI 兼容接口 | DeepSeek/OpenAI/Ollama 一套协议全兼容 |
| 历史 | 前端内存（会话内 3 卡槽） | 刷新清空，最简 |

### 5.2 前端音频链路
`getUserMedia` → 16kHz `AudioContext` → 分两路：① `createAnalyser()`（真实波形，fftSize 128）② `ScriptProcessor` 编码 Int16 PCM → `WebSocket.send(PCM chunk)`（带背压）→ FunASR 流式识别 → 回传 interim/final → 屏幕渲染 + 状态栏更新。

### 5.3 后端启动策略
- `FastWrite-Launcher.bat`（纯 ASCII，避免中文系统代码页解析错误）优先启动 Python 后端；成功 → 掌机走本地引擎。
- Python 不可用 → 仅静态服务 `Handheld.html`（FastWrite-Server.ps1），掌机自动降级 Web Speech API 兜底。

---

## 6. 核心交互流程

1. **启动**：双击 `FastWrite-Launcher.bat` → 浏览器打开 `http://localhost:8964/` = 掌机，徽标显示本地引擎。
2. **录音**：按十字键或 `空格` → 十字变橙红发光、波形随真实音量跳动、屏幕边说边出字（interim 暗 → final 亮）、光标闪烁；状态栏「录制中」。
3. **识别**：音频实时流式上传 → 停下 VAD 分段自动落定 → 状态栏「识别中」。
4. **结束**：再按十字/空格 → 文字自动复制 → 状态栏「已复制」(绿)，SLOT 01 写入（标签 ASR）；去文档 `Ctrl+V`。
5. **热词**：识别总错 → 点卡带翻面维护热词 → 返回后后续识别立即生效（HW 数量更新）。
6. **AI（可选）**：按 `FIX` 纠错 / `POL` 润色 → 「AI处理」→ 结果回写屏幕 + 自动复制 + 覆盖 SLOT 01（标签 FIX/POLISH）；按 `RAW` 对照原文。
7. **历史**：连录多条 → SLOT 01-03；点击某条重复制；`CLEAR` 清空。
8. **取消**：录制中点 `Esc` → 停止、清屏、不复制不记录，toast「已取消，未复制」。

---

## 7. 里程碑与验收标准

| 里程碑 | 内容 | 状态 | 验收标准 |
|---|---|---|---|
| M1 掌机视觉与交互 | 机身/屏幕/翻面/卡带/按键/状态栏 + 真实波形 | ✅ 已达成（待正式验收） | 波形跟随真实音量；状态栏五态齐全；翻面/按压/闪烁到位 |
| M2 前端工程化 | 模块化源码 + 单文件构建 + Provider 抽象 | ⬜ 未做 | 开发目录可维护；构建后仍单 HTML 双击即用；无后端时兜底正常 |
| M3 Python 后端 + 本地识别 | FastAPI + FunASR 流式 + WebSocket + 热词 | ✅ 已完成 | 本地流式首字 <500ms；热词命中率显著提升；VAD 分段正确 |
| M4 LLM 代理 | 纠错 + 润色，DeepSeek/Ollama 可切换 | ✅ 已完成 | 润色保持原意、去口头禅；Key 仅存后端；LLM 不可用静默跳过 |
| M5 验收打磨 | 全链路稳定性、启动器体验、边界与错误处理 | ⬜ 未开始 | 无 Python 全兜底；Win10/11 + Chrome/Edge 通过；取消/断网/权限拒绝等异常路径可恢复 |

---

## 8. 风险与开放问题

| 编号 | 风险/问题 | 应对 |
|---|---|---|
| R1 | paraformer-zh-streaming 权重许可证商用条款不明确 | 开发前确认；必要时改 paraformer-large（apache-2.0） |
| R2 | CPU 性能不达预期（旧机器流式卡顿） | 预留模型规格降级；提供轻量选项 |
| R3 | LLM 润色引入新错误 | 处理基于原始文本、可 `RAW` 对照原文；FIX/POL 手动触发为可选项 |
| R4 | 前端单文件构建链复杂度 | 构建脚本保持最小（纯 Node，不引框架） |
| R5 | 热词表与 LLM 纠错重叠冲突 | 纠错 prompt 注入热词表作为强约束 |
| R6 | 英文热词被标点模型加空格（fastwrite → fast write） | 在 LLM 纠错里将热词作为强约束兜回 |

---

## 9. 本期不做（Out of Scope）

- 说话人分离 / 会议访谈模式
- 中英混合语言识别（本地引擎仅中文）
- 屏幕内编辑（掌机只读，改字在目标文档）
- 历史记录持久化与跨设备同步（仅 SLOT 会话内）
- 「复制全部」批量按钮（掌机仅 SLOT 点击/ CLEAR）
- 移动端 / PWA
- 明暗主题切换（界面即掌机机身形态）
- macOS / Linux 正式支持（后端跨平台，但不作本期验收项）